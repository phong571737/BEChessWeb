# Express backend URL — used only by Next.js server-side rewrites (not needed for runtime)
# Socket.io and API fetch URLs are derived automatically from window.location.hostname at runtime.
API_URL=http://localhost:8080
